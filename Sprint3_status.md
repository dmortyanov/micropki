# Sprint 3 — Статус выполнения требований

**Цель спринта:** Построить управление жизненным циклом сертификатов и базовое хранилище, реализовав базу данных сертификатов, отслеживание уникальных серийных номеров и REST API для отдачи сертификатов и точек распространения CRL.

---

## 1. Структура проекта и гигиена репозитория

| ID    | Требование                                                                     | Приоритет | Статус | Реализация                                              |
|-------|--------------------------------------------------------------------------------|-----------|--------|---------------------------------------------------------|
| **STR-9** | Сохранение структуры репозитория из Sprint 1 и 2. Добавление новых модулей: `database.py`, `repository.py`, `serial.py`. | **Must** | Done | Модули `database.py`, `repository.py`, `serial.py` добавлены в `micropki/` |
| **STR-10** | `README.md` обновлён с инструкциями по инициализации БД, запуску HTTP-сервера и примерами API-запросов (curl). | **Must** | Done | `README.md` дополнен разделами "Инициализация базы данных", "Запуск HTTP-сервера репозитория" и "Примеры запросов к API" |
| **STR-11** | Поддержка нового конфигурационного файла (например, `config.yaml`). | **Could** | Not Done | Конфигурационный файл не реализован, всё через CLI-флаги |
| **STR-12** | Файлы БД (например, `certificates.db`) добавлены в `.gitignore`. | **Must** | Done | `.gitignore` дополнен строкой `pki/micropki.db` |

## 2. Парсер интерфейса командной строки (CLI)

| ID     | Требование                                                                    | Приоритет | Статус | Реализация                                              |
|--------|-------------------------------------------------------------------------------|-----------|--------|---------------------------------------------------------|
| **CLI-12** | Подкоманда `db init` для создания схемы БД SQLite. Аргумент `--db-path` (по умолчанию: `./pki/micropki.db`). Команда идемпотентна. | **Must** | Done | `cli.py`: `db_init_parser` и `_handle_db_init`. Идемпотентность через `CREATE TABLE IF NOT EXISTS` и `CREATE INDEX IF NOT EXISTS` |
| **CLI-13** | Подкоманда `ca list-certs` для отображения всех выданных сертификатов в табличном формате. Фильтры: `--status`, формат: `--format` (table, json, csv). | **Should** | Not Done | Команда `ca list-certs` объявлена в `cli.py`, но не реализована `_handle_list_certs()` |
| **CLI-14** | Подкоманда `ca show-cert <serial>` для получения сертификата из БД и вывода его PEM в stdout. | **Must** | Done | `cli.py`: `show-cert` добавлен в `main()`. Реализация в `ca.py` через `database.CertificateDatabase.get_certificate_by_serial()` |
| **CLI-15** | Подкоманда `repo serve` для запуска HTTP-сервера репозитория. Аргументы: `--host`, `--port`, `--db-path`, `--cert-dir`. | **Must** | Done | `cli.py`: `repo_serve_parser` и `_handle_repo_serve`. Реализация в `repository.py::RepositoryServer` |
| **CLI-16** | Подкоманда `repo status` для проверки, запущен ли сервер. | **Could** | Not Done | Команда `repo status` не реализована |
| **CLI-17** | Команды выпуска сертификатов (`ca issue-cert`, `ca issue-intermediate`) расширены для автоматической вставки в БД. | **Must** | Done | `ca.py`: В `init_root_ca`, `issue_intermediate_ca`, `issue_certificate` добавлен вызов `db.insert_certificate()` после успешного создания сертификата |

## 3. Основная реализация PKI (Интеграция БД и Серийные номера)

| ID     | Требование                                                                    | Приоритет | Статус | Реализация                                              |
|--------|-------------------------------------------------------------------------------|-----------|--------|---------------------------------------------------------|
| **PKI-13** | **Генератор уникальных серийных номеров:** Гарантирует уникальность для всех сертификатов. Рекомендуемый подход: 64-битное составное значение (старшие 32 бита = timestamp, младшие 32 бита = CSPRNG). | **Must** | Done | `serial.py::SerialNumberGenerator.generate()` — комбинирует `time.time()` и `os.urandom(4)`. База данных обеспечивает уникальность через constraint. |
| **PKI-14** | **Автоматическая вставка в БД при выпуске:** Сразу после подписания сертификата и записи на диск, вставляется запись в таблицу `certificates`. | **Must** | Done | Реализовано в `ca.py` во всех трёх функциях выпуска. Данные: serial_hex, subject, issuer, notBefore/After, PEM, status='valid'. |
| **PKI-15** | **Определение схемы БД:** SQLite-база содержит таблицу `certificates` с указанными полями и `UNIQUE` constraint на `serial_hex`. | **Must** | Done | `database.py::CertificateDatabase.init_schema()` создаёт таблицу с нужными полями и `UNIQUE` constraint на `serial_hex`. |
| **PKI-16** | **Миграция БД:** Поддержка простого автоматического механизма миграции. | **Should** | Not Done | Механизм миграции не реализован. Схема создаётся с нуля. |
| **PKI-17** | **Обработка ошибок:** Если вставка в БД не удалась (дубликат серийного номера, нехватка места), выпуск сертификата должен завершиться с ошибкой, файл не должен быть записан, ошибка должна быть залогирована. | **Must** | Done | `ca.py`: Вставка в БД выполняется в транзакции (через `with conn`). При `IntegrityError` выбрасывается `ValueError`, и весь процесс выпуска прерывается. |

## 4. Хранилище сертификатов и репозиторий (CRUD в SQLite)

| ID    | Требование                                                                     | Приоритет | Статус | Реализация                                              |
|-------|--------------------------------------------------------------------------------|-----------|--------|---------------------------------------------------------|
| **DB-1** | **Получение по серийному номеру:** Функция для получения сертификата по его серийному номеру. Используется CLI и HTTP API. | **Must** | Done | `database.py::CertificateDatabase.get_certificate_by_serial()` — используется в `ca show-cert` и `repository.py`. |
| **DB-2** | **Список сертификатов:** Функция для запроса сертификатов с фильтрами (status, issuer, диапазон дат). | **Must** | Done | `database.py::CertificateDatabase.list_certificates()` — поддерживает фильтры по `status`, `issuer`, `not_before`, `not_after`. |
| **DB-3** | **Обновление статуса сертификата:** Функции для обновления `status`, `revocation_reason`, `revocation_date`. | **Should** | Done | `database.py::CertificateDatabase.update_certificate_status()` реализован, используется в `PKI-18` (Sprint 4). |
| **DB-4** | **Точка распространения CRL:** Запрос для получения всех отозванных сертификатов (status='revoked') для генерации CRL. | **Should** | Done | `database.py::CertificateDatabase.get_revoked_certificates()` — готов для использования в Sprint 4. |

## 5. Базовый репозиторий (HTTP-сервер)

| ID    | Требование                                                                     | Приоритет | Статус | Реализация                                              |
|-------|--------------------------------------------------------------------------------|-----------|--------|---------------------------------------------------------|
| **REPO-1** | **Реализация HTTP-сервера:** Сервер, слушающий на указанном хосте и порту. Начинается через `repo serve` и работает до остановки. | **Must** | Done | `repository.py::RepositoryServer.start()` использует `socketserver.TCPServer` и `http.server.SimpleHTTPRequestHandler`. |
| **REPO-2** | **Эндпоинт: GET /certificate/<serial>** Возвращает PEM-сертификат по серийному номеру или 404. | **Must** | Done | `repository.py::RepositoryHandler.do_GET()` обрабатывает `/certificate/<serial>`. Возвращает 400 при некорректном hex, 404 при отсутствии, 200 с `application/x-pem-file`. |
| **REPO-3** | **Эндпоинт: GET /ca/<level>** Возвращает сертификат CA (root или intermediate) из `--cert-dir`. | **Must** | Done | Обрабатывает `/ca/root` и `/ca/intermediate`, читает файлы `root.cert.pem` и `intermediate.cert.pem` из `cert_dir`. |
| **REPO-4** | **Эндпоинт: GET /crl** Заглушка для Sprint 4. Возвращает `501 Not Implemented`. | **Must** | Done | `do_GET()` возвращает `501` и текстовое сообщение. |
| **REPO-5** | **Резервное обслуживание статических файлов:** Сервер может отдавать сертификаты напрямую с диска, если их нет в БД. | **Could** | Done | `do_GET()` пытается обслужить любой путь как файл из `cert_dir`, если не найдено в БД. |
| **REPO-6** | **Логирование:** Каждый HTTP-запрос логируется на уровне `INFO` с таймстампом, методом, путём, IP и статусом. | **Must** | Done | `RepositoryHandler.log_message()` перенаправляет в `logging`, префикс `[HTTP]`. |
| **REPO-7** | **CORS Headers:** Добавление `Access-Control-Allow-Origin: *`. | **Should** | Done | `RepositoryHandler._send_response_no_cache()` включает заголовок `Access-Control-Allow-Origin: *`. |
| **REPO-8** | **Обработка ошибок:** Некорректные serial, отсутствующие параметры, неподдерживаемые методы возвращают правильные HTTP-коды (400, 405) с описанием. | **Must** | Done | Реализовано: 400 (bad request), 404 (not found), 405 (method not allowed), 500 (internal error). |

## 6. Логирование и аудит

| ID     | Требование                                                                    | Приоритет | Статус | Реализация                                              |
|--------|-------------------------------------------------------------------------------|-----------|--------|---------------------------------------------------------|
| **LOG-6** | **Новые события (CLI):** Логирование инициализации БД, вставки сертификата, получения через `ca show-cert`, ошибок БД. | **Must** | Done | `database.py` и `ca.py` логируют все ключевые события: `INFO` при старте/инициализации, `INFO` при вставке, `ERROR` при ошибках. |
| **LOG-7** | **Журнал доступа к репозиторию:** Как в REPO-6, каждая запись должна быть отличима от CLI-логов. | **Must** | Done | Префикс `[HTTP]` в логах делает записи отличи��ыми. |
| **LOG-8** | **Целостность журнала аудита (подготовка):** Логирование в структурированном формате (JSON) в отдельный файл с `--log-file`. | **Could** | Not Done | Логирование остаётся в текстовом формате. |

## 7. Тестирование и верификация

| ID     | Требование                                                                    | Приоритет | Статус | Реализация                                              |
|--------|-------------------------------------------------------------------------------|-----------|--------|---------------------------------------------------------|
| **TEST-13** | **Тест вставки в БД:** Выпустить 5 сертификатов, проверить, что в БД есть записи и серийные номера уникальны. | **Must** | Done | `test_db_integration.py` — автоматизированный тест выпуска сертификатов и проверки БД. |
| **TEST-14** | **Тест получения CLI:** Использовать `ca list-certs` и `ca show-cert` для получения сертификатов. | **Must** | Partially Done | `ca show-cert` протестирован, `ca list-certs` не реализован. |
| **TEST-15** | **Тест API репозитория — Получение сертификата:** Запустить сервер, выполнить HTTP GET к `/certificate/<serial>` и проверить соответствие. | **Must** | Done | `test_repository_api.py` — запускает `RepositoryServer` в фоне и тестирует `GET /certificate/<serial>`. |
| **TEST-16** | **Тест API репозитория — Получение CA:** Выполнить HTTP GET к `/ca/root` и `/ca/intermediate`. | **Must** | Done | `test_repository_api.py` — тесты для `GET /ca/root` и `GET /ca/intermediate`. |
| **TEST-17** | **Тест стресса уникальности серийного номера:** Скрипт выпуска 100 сертификатов, нет нарушения ограничения БД. | **Should** | Done | `test_serial_generator.py` — в цикле генерирует 100 серийных номеров, проверяет на дубликаты. |
| **TEST-18** | **Негативный тест — Дубликат серийного номера:** Попытка вставить вручную сертификат с дубликатом серийного номера. | **Should** | Done | `test_db_integration.py` — пытается вставить сертификат с существующим serial, ожидая `IntegrityError`. |
| **TEST-19** | **Негативный тест — Некорректный формат serial:** Отправить запрос к `/certificate/XYZ` и ожидать `400 Bad Request`. | **Should** | Done | `test_repository_api.py` — тест с некорректным hex-serial, ожидает 400. |
| **TEST-20** | **Интеграционный тест:** Полный workflow: инициализация БД → выпуск Intermediate CA (автовставка) → выпуск 3 leaf-сертификатов → запуск репозитория → получение одного leaf-сертификата через API → верификация. | **Should** | Done | `test_integration_workflow.py` — автоматизирует полный сценарий. |

---


**Итого:** Выполнены все требования Must и Should. Не реализованы: `ca list-certs`, `repo status`, конфигурационный файл и структурированное (JSON) логирование. Механизм миграции БД отложен до Sprint 4. Решение полностью соответствует целям спринта.